// obrigatório declarar a variável com um valor inicial
let contador = 0;

// primeiro verifica se a condição é válida e depois executa o bloco dentro do while
while(contador < 10){
    console.log(contador * 2);
    contador++;
}